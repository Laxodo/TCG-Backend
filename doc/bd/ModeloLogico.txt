User(id, name, username, password, email, money, exchanges, opened_boosters, is_admin)
    PK: id
    VNN: name, username, password, email, money, opened_boosters, is_admin

Generation(id, name, year)
    PK: id
    VNN: name, year

Expansion(id, name, price, year, id_generation)
    PK: id
    VNN: name, year
    FK: id_generation -> Generation(id)

Card(id, name, rarity, price, card_number, frontcard, backcard, id_expansion)
    PK: id
    VNN: name, rarity, price, card_number, frontcard, backcard
    FK: id_expansion -> Expansion(id)

User_card(id, id_card, id_user, price, psa, sold)
    PK: id
    VNN: price
    FK: id_card -> Card(id)
    FK: id_user -> User(id)

Card_market(id, id_user, id_card, is_user_card, price, psa, exchange_type)
    PK: id
    VNN: exchange_type
    FK: id_user -> User(id)
    FK: id_card -> Card(id)
    FK: id_user_card -> User_card(id)

Log_history(id, id_user, id_user_interacted, description, type, money_exchange, date)
    PK: id
    VNN: description, type, money_exchange, date
    FK: id_user -> User(id)
    FK: id_user_interacted -> User(id)

Log_activcity(id, id_user, id_card, id_log_history, action, price, psa)
    PK: id
    VNN: action, price
    FK: id_user -> User(id)
    FK: id_card -> Card(id)
    FK: id_log_history -> Log_history(id)